import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 36,
    paddingTop: 50,
  },
  input: {
    marginBottom: 10,
  },
});
